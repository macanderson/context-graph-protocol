[package]
name = "contextgraph-refprov"
description = "Shared Context Graph Protocol stdio skeleton for the reference providers (contextgraph-ripgrep, contextgraph-treesitter). Internal to this repo; not published."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
# Internal helper crate shared by the reference-provider binaries; stays
# unpublished (the workspace default), it is not part of the released product.
publish = false

[dependencies]
contextgraph-types = { path = "../contextgraph-types" }
contextgraph-host = { path = "../contextgraph-host" }
serde_json.workspace = true
sha2.workspace = true
