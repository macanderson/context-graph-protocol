version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Re-pin shared governance workflows across all callers together (oxagen#2989).
    ignore:
      - dependency-name: "macanderson/oxagen/.github/workflows/*"
